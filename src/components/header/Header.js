import classes from './Header.module.css'
const Header = () => {
    return(
        <div className={classes.container}>
            <h1 className={classes.header}>Transfer GROSS to NET and vice versa</h1>
        </div>
    )
}

export default Header;